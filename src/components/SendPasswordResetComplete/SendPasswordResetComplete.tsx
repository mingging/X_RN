import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const SendPasswordResetComplete = (props: Props) => {
  return (
    <View>
      <Text>SendPasswordResetComplete</Text>
    </View>
  );
};

export default SendPasswordResetComplete;
