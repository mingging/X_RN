import CommonStackNavigation from '@routes/components/CommonStackNavigation';

import React, {useCallback, useEffect} from 'react';
import {View, Text} from 'react-native';

interface Props {}

const CommonStackNavigationContainer = (props: Props) => {
  return <CommonStackNavigation />;
};

export default CommonStackNavigationContainer;
